import React, { Fragment } from 'react';
import Analyses from '../analyses/Analyses';

const Home = () => {
  return (
    <Fragment>
      <Analyses></Analyses>
    </Fragment>
  );
};

export default Home;
